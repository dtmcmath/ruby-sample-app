<h1><%= @title %></h1>
<%= form_for(@user) do |f| %>
  <%= render 'userinfo_form', :f => f, :button_title => 'Update' %>
<% end %>
<div>
	<%= gravatar_for @user %>
	<%= link_to 'change', 'http://gravatar.com/emails', :target => '_blank' %>
</div>
